# ---------------------------------------------------------------------------
# AsyncFlow Engine — Python Dependencies
# Pin minor versions for reproducibility; allow patch updates.
# ---------------------------------------------------------------------------

# --- Web Framework ----------------------------------------------------------
fastapi==0.115.*
uvicorn[standard]==0.30.*          # ASGI server with HTTP/1.1 + WebSocket support

# --- Data Validation --------------------------------------------------------
pydantic==2.8.*
pydantic-settings==2.4.*           # .env file loading for Settings

# --- Queue / Redis ----------------------------------------------------------
rq==1.16.*                         # Redis Queue — simple, robust job queue
redis==5.0.*                       # Async-compatible Redis client

# --- LLM Client (Phase 2) ---------------------------------------------------
httpx==0.27.*                      # Async HTTP client for Ollama API calls

# --- Dev / Testing ----------------------------------------------------------
pytest==8.3.*
pytest-asyncio==0.23.*
httpx                              # Also used by TestClient
